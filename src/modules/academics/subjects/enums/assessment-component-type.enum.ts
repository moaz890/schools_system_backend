/**
 * Bucket types for graded work; used by assessment profiles and (later) exams/assignments.
 */
export enum AssessmentComponentType {
  EXAM = 'EXAM',
  QUIZ = 'QUIZ',
  ASSIGNMENT = 'ASSIGNMENT',
  ATTENDANCE = 'ATTENDANCE',
  BEHAVIOR = 'BEHAVIOR',
}
